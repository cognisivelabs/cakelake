One-line: the one deliberately illustrated moment in the system — a cake gaining a layer per completed order stage.

```jsx
<OrderTracker orderId="ORDER #LY-4471" eta="READY ~16:40" current={1}
  footer={<Button variant="inverse">Simulate next stage</Button>} />
```

Always sits on `--ink-raised` inside a full-width `--ink` block. Cake layers light up honey → pistachio → berry → candle. Never reuse the cake illustration as generic decoration.
