One-line: the standard section opener — use it instead of a bare `<h2>` so eyebrow/heading rhythm stays consistent.

```jsx
<SectionHead eyebrow="Menu" title="Pick your bake" description="Baked the morning you collect."
  action={<Button variant="ghost" size="sm">See all</Button>} />
```

Pass `tone="dark"` inside ink sections.
