One-line: category filter row on the menu; a horizontal wrap of pills where exactly one is active.

```jsx
<div style={{display:'flex',gap:8,flexWrap:'wrap'}}>
  <Tab active>All bakes</Tab><Tab>Celebration cakes</Tab><Tab>Cupcakes</Tab>
</div>
```
